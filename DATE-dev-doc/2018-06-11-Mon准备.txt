2018-06-11 Mon 准备

trace-ListStringIfFor-849~876不配对.txt


怎么着算是“在循环中走出了比较新的执行”？

+ 循环进行的次数……
+ 通过 对循环语句块执行之后的影响 来间接反映？


非 break、return、goto 的，循环的最后一次比较肯定是不满足（对应到字节码里则一般是“满足”）

break 掉的话……


    int n = 3, m = 0;
    while (n > 0) {
      n--;
      m++;
      if(m>2){
          break;
      }
    }

分支覆盖率有何不可？

每个点儿
接近了
远离了